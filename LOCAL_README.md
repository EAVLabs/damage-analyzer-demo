# Damage Analyzer — Local Setup

## Prerequisites

- Python 3.11+
- An OpenAI API key

## Setup

```bash
pip install -r requirements.txt
cp .env.example .env
```

Add your `OPENAI_API_KEY` to the `.env` file.

## Run

```bash
python run.py
```

Open `http://localhost:5000`.
