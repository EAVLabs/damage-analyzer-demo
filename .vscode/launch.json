{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run with Gunicorn",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/run.py",
            "console": "integratedTerminal",
            "env": {
                "FLASK_ENV": "development"
            }
        }
    ]
}
